# 一键上传到 GitHub（保留远端文件）

目标仓库：https://github.com/L-kin-trim/CUMCM2026

双击本目录的 `一键上传GitHub.cmd`。出现 `Sync successful.` 表示上传成功。

## 上传规则

- 本地新增文件：上传。
- 本地已有文件被修改：更新 GitHub 上的对应文件。
- 本地删除文件：忽略删除，GitHub 上的文件继续保留，本地也不会自动恢复文件。
- 本地重命名或移动文件：上传新路径，GitHub 上的旧路径继续保留。
- 无变化的文件：不产生新提交。
- Git 内部目录、空目录及被 Git 忽略的文件不上传。

脚本先获取远端状态，然后重建暂存区，仅暂存新增和修改。已有的暂存选择会被重新计算，包括取消暂存的删除；磁盘文件不因此改变。执行前请完成其他 Git 操作。

提交前检查删除记录；推送前检查已有的待推送提交。如果其中包含删除文件的历史（包括重命名造成的旧路径删除），停止上传，避免将删除带到 GitHub。脚本不使用强制推送。

本地已删除但远端保留的文件仍可能在 `git status` 中显示为删除，这是预期状态，不影响后续新增和修改的上传。

## 失败处理

远端有本地尚未包含的提交时，脚本停止并提示先合并远端更新；网络或权限失败时，处理提示后重试。待推送历史包含删除时，应先检查相关本地提交，不要直接手动推送绕过保护。

这些保护约束本脚本的上传行为；此前已经同步的删除不会自动恢复，手动 Git 操作或其他工具也不受本脚本约束。

启动脚本采用 ASCII 编码和 Windows CRLF 换行，避免命令截断和中文乱码。需安装 Git 并具有仓库写入权限。
